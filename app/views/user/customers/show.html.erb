<div class="d-flex justify-content-center">
  <h1>会員情報</h1>
</div>

<div class="d-flex justify-content-center">

  <div class="p-2 bd-highlight">
    <div class=" h3" style="width:500px;">
      <table class="table">
        <tbody>
          <tr>
            <td class="table-active">会員ID </td>
            <td><%= @user.id %></td>
          </tr>
          <tr>

          <tr>
            <td class="table-active">ニックネーム</td>
            <td><%= @user.nickname %></td>
          </tr>

          </tr>
            <td class="table-active">メールアドレス</td>
            <td><%= @user.email %></td>
        </tbody>
      </table>
      <%= link_to "退会する", customer_confirmation_path(current_user) %>
    </div>
  </div>

<div class=" mt-3 overflow-auto bd-highlight">
    <h2>
      投稿一覧
      <%= link_to "投稿", new_tweet_path %>
    </h2>
    <h3><%= link_to "みんなの投稿を見る", everyone_tweet_path %></h3>
    <div class=" mt-3 overflow-auto bd-highlight" style="height:600px;">
      <% @tweets.each do |tweet| %>
        <div class="border rounded border-dark p-2 my-2 h4 "style="width:500px;">
          <p>
            <%= image_tag tweet.get_image(200,200) %>
            <%= tweet.sub_sentence %>
            <h6><%= tweet.created_at.strftime('%Y/%m/%d') %></h6>
            <%= link_to "編集", edit_tweet_path(tweet.id) %>
          </p>
        </div>
      <% end %>
  </div>
  </div>
  </div>


